import React from "react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export default function LegalTechHome() {
  return (
    <div className="min-h-screen bg-gray-100 flex flex-col items-center p-6">
      <h1 className="text-3xl font-bold text-blue-600">LegalTech Solutions</h1>
      <p className="text-lg text-gray-700 mt-2">Empowering Legal Services with Technology</p>
      
      {/* Services Section */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mt-6">
        {["Contract Review", "Legal Consultations", "Business Compliance"].map((service, index) => (
          <Card key={index} className="p-4 text-center shadow-md">
            <CardContent>
              <h2 className="text-xl font-semibold">{service}</h2>
              <p className="text-gray-600 mt-2">Expert {service.toLowerCase()} services.</p>
              <Button className="mt-4">Learn More</Button>
            </CardContent>
          </Card>
        ))}
      </div>
      
      {/* Contact Form */}
      <div className="mt-8 bg-white p-6 rounded-lg shadow-md w-full md:w-1/2">
        <h2 className="text-2xl font-bold mb-4">Contact Us</h2>
        <Input placeholder="Your Name" className="mb-4" />
        <Input placeholder="Your Email" className="mb-4" />
        <Button className="w-full">Send Message</Button>
      </div>
    </div>
  );
}
